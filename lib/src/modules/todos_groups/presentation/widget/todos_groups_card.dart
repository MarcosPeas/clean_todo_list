import 'package:flutter/material.dart';
import 'package:todo_list/src/modules/todos_groups/domain/entities/todo_group.dart';
/*
class TodosGroupsCard extends StatelessWidget {
  final TodoGroup todoGroup;
  final void Function(int index, TodoGroup todoGroup) onTap;

  const TodosGroupsCard({
    Key? key,
    required this.todoGroup,
    required this.onTap,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListTile(
      title: Text('${todoGroup.title}: ${todoGroup.createdAt.toString()}'),
      subtitle: Text(todoGroup.subtitle),
      trailing: onTap(indexGroup: index, todoGroup: todoGroup),
    );
  }
}*/
